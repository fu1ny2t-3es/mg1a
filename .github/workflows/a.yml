on:
  workflow_dispatch:
  schedule:
    - cron: '45 15 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/mg1a@cron
        with:
          token: ${{ secrets.token }}
